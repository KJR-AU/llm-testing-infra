{
    "name": "KJR - llm-testing-infra",
    "build": {
      "dockerfile": "Dockerfile",
      "args": {
        "VARIANT": "3.11-bookworm"
      }
    },
    "runArgs": [
        "--env-file", "${localWorkspaceFolder}/.devcontainer/.env"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "hashicorp.terraform",
                "openai.chatgpt"
            ],
            "settings": {
                "terraform.formatOnSave": true,
                "terraform.lintOnSave": true,
                "terraform.useLanguageServer": true,
                "terminal.integrated.shell.linux": "/bin/bash"
            }
        }
    },
    "features": {
        "ghcr.io/devcontainers/features/terraform:1": {
            "version": "1.11.4"
        },
        "ghcr.io/devcontainers/features/aws-cli": {
            "version": "latest"
        },
        "ghcr.io/devcontainers/features/kubectl-helm-minikube:1": {
            "version": "1.33.0",
            "helmVersion": "latest"
        }
    },
    "mounts": [
        "source=${localEnv:HOME}/.aws,target=/home/vscode/.aws,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.ssh,target=/home/vscode/.ssh,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.kube/config,target=/home/vscode/.kube/config,type=bind,consistency=cached"
    ],
    "postCreateCommand": ["terraform", "-chdir=terraform", "init"],
    "remoteUser": "vscode"
  }
  